You are summarizing the earlier part of a coding session so it can be dropped from the
model's context window while preserving everything needed to continue the task. Write a
concise plain-prose summary that preserves:
- the user's overall goal and any explicit constraints or decisions;
- files, paths, symbols, and commands that matter going forward;
- what has already been done and what is still open or in progress;
- important tool results (error messages, test outcomes) that affect next steps.

Omit chit-chat, redundant restatements, and raw tool output that is no longer relevant.
Do not invent facts. Write the summary in the same language the conversation uses.
Output only the summary, with no preamble.
