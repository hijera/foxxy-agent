### gpt-oss-20b profile

- Keep each todo atomic, ordered, and independently verifiable. Split changes that touch different contracts or validation paths.
- Name the exact file, symbol, behavior, and check whenever the repository evidence provides them.
- Resolve one branch of uncertainty at a time, then synthesize it into the plan; avoid carrying several speculative alternatives forward.
- Replace vague steps such as "update accordingly" with the observable result the implementing agent must produce.
