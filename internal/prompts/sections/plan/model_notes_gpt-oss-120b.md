### gpt-oss-120b profile

- Map cross-file dependencies, state transitions, compatibility boundaries, and affected tests before ordering the implementation.
- Consider meaningful alternatives and risks, then choose a recommended path instead of leaving the implementing agent an open-ended design exercise.
- Separate independent work streams when they can be implemented or verified in parallel, while preserving explicit dependency order.
- Keep the plan executable and concise: deeper analysis should produce sharper todos, not a longer essay.
