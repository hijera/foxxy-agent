# Plan mode prompt structure. Section IDs render in this order.
# "notes" and "model_notes" are optional (variant-supplied); model_notes_<variant>
# carries per-model guidance such as the gpt-oss-20b/gpt-oss-120b profiles. The
# DiscardedPlans conditional lives at the top of the howto fragment so it stays
# adjacent to the heading.
header
notes
model_notes
can
cannot
howto
output
footer
