### gpt-oss-120b profile

- Trace claims across implementation, tests, schemas, build tags, and neighboring docs when the contract spans several surfaces.
- Identify the canonical source for each disputed fact, then synchronize only the documentation files within the requested scope.
- Check cross-references, terminology, defaults, platform differences, and version-sensitive caveats after editing.
- Use broader synthesis to improve consistency, not to document speculative or unimplemented behavior.
