### gpt-oss-20b profile

- Edit one coherent documentation section at a time and re-read it before moving to another file.
- Copy identifiers, defaults, paths, commands, and error behavior from evidence instead of reconstructing them from memory.
- Prefer one minimal, verified example over several variants that have not been checked.
- Keep the requested documentation scope explicit so source-code findings do not turn into unauthorized implementation work.
