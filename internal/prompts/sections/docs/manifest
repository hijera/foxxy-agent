# Docs mode prompt structure. Section IDs render in this order.
# "notes" and "model_notes" are optional (variant-supplied); model_notes_<variant>
# carries per-model guidance such as the gpt-oss-20b/gpt-oss-120b profiles.
header
notes
model_notes
intent
facts
editing
writing
verification
final
footer
